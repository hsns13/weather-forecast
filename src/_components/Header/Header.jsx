import React, { Fragment } from 'react';

export const Header = (props) => {
    return (
        <Fragment>
            <div className="heading">
                Weather forecast...
            </div>
        </Fragment>
    )
};